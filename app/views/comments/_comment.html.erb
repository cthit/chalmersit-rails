<li class="comment">
  <article class="row">
    <header class="row">
      <figure class="large-2 columns"><%= image_tag 'comment.user.avatar' %></figure>
      <div class="large-10 columns">
        <h3 class="fn"><%= comment.user_id %></h3> <% 'comment.user.full_name' %>
        <p class="comment-meta">
          <time title="<%= comment.created_at.to_s(:iso_date_time) %>" datetime="<%= comment.created_at.to_s(:rfc822) %>" pubdate><%= distance_of_time_in_words_to_now comment.created_at %></time>
        </p>
      </div>
    </header>
    <div class="comment-text large-10 columns large-offset-2">
      <%= markdown comment.body %>
    </div>
  </article>
</li>