<%= simple_form_for @contact, url: contact_index_path, method: :post do |f| %>

  <%= f.error_notification %>

  <%= f.input :title, label: 'Subject' %>
  <%= f.input :body, placeholder: 'What do you want to say?', input_html: { rows: 10 } %>
  <%= f.input :email, as: :email, label: 'Email (Leave empty if you want to be anonymous)',
  	 	placeholder: 'user@domain.com' %>
  <%= f.label 'Who do you want to contact?' %>
  <%= f.collection_check_boxes :to_whom, [['styrit@chalmers.it', 'styrIT'], 
  		['samo@chalmers.it', 'SAMO'], ['snit@chalmers.it', 'snIT'], 
  		['valberedningen@chalmers.it', 'Valberedningen']], :first, :last %>
  <%= f.button :submit %>

<% end %>