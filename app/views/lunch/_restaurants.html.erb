<% restaurants.each do |restaurant| %>
  <h3><%= restaurant[:name] %></h3>
  <dl>
    <% restaurant[:meals].each do |meal| %>
    <dt><%= meal[:title] %><span class=allergens><%= meal[:allergens] %></span></dt>
    <dd><%= meal[:summary] %></dd>
    <% end %>
  </dl>
<% end %>
