<% content_for :title, t('helpers.submit.update', model: Committee.model_name.human.downcase) %>

<%= render 'form' %>

<%= link_to t('show'), @committee %> |
<%= link_to t('back'), committees_path %>
