<% content_for :sidebar_left do %>
  <%= render 'shared/markdown_help' %>
<% end %>

<% content_for :sidebar_right do %>
  <%= render 'shared/file_list' %>
<% end %>

<%= simple_form_for(@page, html: { novalidate: true }) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <%= render "localized_fields", f: f %>

  <%= f.input :parent_id, as: :select, collection: @other_pages , value_method: :id, label_method: :title_sv, include_blank: true, label: Page.human_attribute_name(:parent) %>
  <%= f.button :submit %>
<% end %>

<%= render "shared/markdown_preview" %>
