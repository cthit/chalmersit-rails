<% content_for :title, t('helpers.submit.update', model: Page.model_name.human.downcase) %>

<%= render 'form' %>

<%= link_to t('delete'), @page, method: :delete, data: { confirm: t('confirm') } %> |
<%= link_to t('show'), path_to_page(@page) %> |
<%= link_to t('back'), pages_path %>
