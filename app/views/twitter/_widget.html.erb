<% cache do %>
  <section class="box widget it_twitter" data-handle="<%= handle %>">
    <header class="panel-header">
      <h1>Twitter</h1>
      <%= link_to "@#{handle}", "http://twitter.com/#{handle}", class: 'header-more' %>
    </header>
    <div class="tweet-list"</div>
  </section>
<% end %>
