<% content_for :title, t('helpers.submit.update', model: Post.model_name.human.downcase) %>

<%= render 'form' %>

<%= link_to t('delete'), @post, method: :delete, data: { confirm: t('confirm') }, class: 'button expand alert' %>
<%= link_to t('show'), @post %> |
<%= link_to t('back'), posts_path %>
